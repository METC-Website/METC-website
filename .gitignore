# Local systems and tooling
.DS_Store
.playwright-cli/
*.tsbuildinfo
~$*.doc*
~$*.ppt*

# Install and build output
.next/
node_modules/
out/
dist/
coverage/
output/
*.log
__pycache__/
*.pyc

# Local environment files. Only the redacted example is versioned.
.env
.env.local
.env.*.local
.env.worker.local
!.env.example

# Unreviewed source material outside the repository and the Vercel-excluded resource tree.
/resources/
/public/resources/

# This repository uses pnpm; its lockfile is tracked.
# Avoid introducing lockfiles from other package managers.
yarn.lock
package-lock.json
